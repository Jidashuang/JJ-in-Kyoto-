// Image asset stubs. Returning undefined so consumers fall through to their
// declared placeholder paths. When real curated imagery lands, replace these
// with real lookups (likely keyed by place slug, not index).

export function placeHeroImage(): string | undefined {
  return undefined;
}

export function placeGalleryImage(): string | null | undefined {
  return undefined;
}
